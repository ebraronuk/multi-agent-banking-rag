# Backend URL — the FastAPI service from ../docker/docker-compose.yml (or
# `make dev` in the repo root). Must match the origin allowed by the
# backend's CORS_ALLOWED_ORIGINS (see ../.env.example).
NEXT_PUBLIC_API_URL=http://localhost:8000
